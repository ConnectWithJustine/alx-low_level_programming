#include "operation.h"
/**
 * mod - a function that return the remainder of a number
 * @a: first number
 * @b: second number
 *
 * Return: 0 if successful else 1
 */

int mod(int a, int b)
{
	return (a % b);
}
